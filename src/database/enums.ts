export const USER_TYPES = ["standard", "creator", "admin"] as const

export const GATE_TYPES = [
    "north",
    "east",
    "south",
    "west",
    "up",
    "down",
    "other",
] as const

export const ITEM_TYPES = ["basic", "key"] as const

export const ASSET_RESOURCE_TYPES = [
    "grids",
    "tiles",
    "events",
    "characters",
    "items",
] as const

export const ASSET_TYPES = ["images"] as const
